# Typscrap class file

```
lib.typ
document.typ
theme/   # Typscrap class のテーマに関するところ
  colors.typ
rule/    # show rule の定義
  lib.typ
  create.typ
component/
  lib.typ  # component の公開 API
  code.typ
  ...
internal/
  content.typ
target/
  html.typ
```
